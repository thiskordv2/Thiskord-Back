name: Build and Deploy image to DigitalOcean

on:
  pull_request:
    types:
      - closed
    
permissions: 
  contents: read
  packages: write
        
jobs:
  deploy:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - name: Chekout repository
        uses: actions/checkout@v4
      - name: Log in to Container Registry
        uses: docker/login-action@v3.3.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and Push Dockerimage to GitHub Container Registry
        uses: docker/build-push-action@v4.3.0
        with:
          context: .
          push: true
          tags: ghcr.io/${{ github.repository }}:latest
      # - name: Deploy app with ssh
      #  uses:
      #  with:
      #      host: ${{ secrets.SERVER_HOST }}
      #      username: ${{ secrets.SERVER_USER }}
      #      key: ${{ secrets.SERVER_SSH_KEY }}
      #  run: docker compose down -f compose-cd.yaml
      #  run: git pull origin main
      #  run: docker compose pull && docker compose up -f compose-cd.yaml 